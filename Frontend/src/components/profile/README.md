# Profile components

**Owner: Raul Ja'aim Everts (230270565)**

Components used only by profiles (/profile, /profile/:userId) live here, so nobody else's work touches them.

Anything genuinely reusable across pages belongs in `../ui/` instead — and if
you move something there, say so in the group chat so the others can use it.
