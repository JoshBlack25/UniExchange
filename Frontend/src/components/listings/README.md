# Listing components

**Owner: Aidan Barends (230255639) + Mogamat Wazeer Gilbert (221374698)**

Components used only by listing details (/listings/:listingId) and create listing (/listings/new) live here, so nobody else's work touches them.

Anything genuinely reusable across pages belongs in `../ui/` instead — and if
you move something there, say so in the group chat so the others can use it.
